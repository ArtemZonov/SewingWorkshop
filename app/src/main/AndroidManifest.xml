<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jkdajac.sewingworkshop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SewingWorkshop">
        <activity android:name=".clients.EditActivity" />
        <activity android:name=".clients.ClientsActivity" />
        <activity android:name=".manual.RashodTkaneiActivity" />
        <activity android:name=".manual.FormuliActivity" />
        <activity android:name=".manual.MerkiActivity" />
        <activity android:name=".manual.DefektiActivity" />
        <activity android:name=".manual.PribavkiActivity" />
        <activity android:name=".manual.ManualActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>